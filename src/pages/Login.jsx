import React,{useState} from 'react'
import {useNavigate} from 'react-router-dom'
export default function Login(){const [email,setEmail]=useState('');const [password,setPassword]=useState('');const [message,setMessage]=useState(null);const navigate=useNavigate();function handleSubmit(e){e.preventDefault();if(email.includes('@')&&password.length>=4){setMessage({type:'success',text:'Login successful'});setTimeout(()=>navigate('/'),800)}else setMessage({type:'error',text:'Login failed — check credentials'})}return (<div className="max-w-md mx-auto px-6 py-20"><h2 className="text-2xl font-semibold mb-4">Login</h2>{message&&<div className={`p-3 mb-4 rounded ${message.type==='success'?'bg-green-100 text-green-800':'bg-red-100 text-red-800'}`}>{message.text}</div>}<form onSubmit={handleSubmit} className="space-y-4"><input value={email} onChange={e=>setEmail(e.target.value)} className="w-full border rounded p-3" placeholder="Email" /><input type="password" value={password} onChange={e=>setPassword(e.target.value)} className="w-full border rounded p-3" placeholder="Password" /><button className="bg-primary text-white px-6 py-3 rounded">Login</button></form></div>)}
